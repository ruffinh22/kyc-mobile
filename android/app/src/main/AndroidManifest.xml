<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest.xml — KYC Mobile
  Permissions clés :
    - CAMERA / RECORD_AUDIO     → appel vidéo WebRTC
    - FOREGROUND_SERVICE        → maintien de l'appel en arrière-plan
    - USE_FULL_SCREEN_INTENT    → écran d'appel sur écran verrouillé (Android 10+)
    - RECEIVE_BOOT_COMPLETED    → re-register FCM token au démarrage
    - VIBRATE / WAKE_LOCK       → sonnerie & réveil écran
    - CALL_PHONE / READ_CALL_LOG → CallKeep Android Telecom
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kycmobile">

  <!-- ═══ PERMISSIONS RÉSEAU ═══ -->
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

  <!-- ═══ PERMISSIONS MÉDIA ═══ -->
  <uses-permission android:name="android.permission.CAMERA" />
  <uses-permission android:name="android.permission.RECORD_AUDIO" />
  <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
  <uses-permission android:name="android.permission.BLUETOOTH" />
  <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

  <!-- ═══ SONNERIE & RÉVEIL ═══ -->
  <uses-permission android:name="android.permission.VIBRATE" />
  <uses-permission android:name="android.permission.WAKE_LOCK" />
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
  <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />

  <!-- ═══ NOTIFICATIONS (Android 13+ / API 33) ═══
       Obligatoire depuis Android 13 : sans cette déclaration, la demande
       d'autorisation faite dans NotificationService.ts est silencieusement
       ignorée et l'utilisateur ne verra jamais l'invite système. -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

  <!-- ═══ APPEL SUR ÉCRAN VERROUILLÉ ═══ -->
  <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />

  <!-- ═══ FOREGROUND SERVICE — TYPES CAMÉRA/MICRO (obligatoire API 34+) ═══
       Depuis Android 14, un foreground service qui capture caméra/micro doit
       déclarer explicitement ces permissions, en plus de son type déclaré
       sur le service lui-même (voir KycForegroundCallService ci-dessous).
       Sans elles, le service est arrêté par le système lors du passage en
       arrière-plan pendant un appel actif. -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

  <!-- ═══ CALLKEEP (Android Telecom API) ═══ -->
  <uses-permission android:name="android.permission.CALL_PHONE" />
  <uses-permission android:name="android.permission.READ_CALL_LOG" />
  <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />

  <!-- ═══ CARACTÉRISTIQUES MATÉRIELLES ═══ -->
  <uses-feature android:name="android.hardware.camera"        android:required="true" />
  <uses-feature android:name="android.hardware.camera.front"  android:required="false" />
  <uses-feature android:name="android.hardware.microphone"    android:required="true" />
  <!-- L'app utilise l'API Telecom (CallKeep) sans être un dialer téléphonique :
       la déclarer non-requise élargit la compatibilité (tablettes, certains
       appareils Wi-Fi-only sans modem cellulaire). -->
  <uses-feature android:name="android.hardware.telephony" android:required="false" />

  <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:networkSecurityConfig="@xml/network_security_config"
      android:largeHeap="true">

    <!-- ── Activité principale ─────────────────────────────────────────── -->
    <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true"
        android:showOnLockScreen="true"
        android:showWhenLocked="true"
        android:turnScreenOn="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <!-- ── Service CallKeep : gestion des messages en arrière-plan ────────────── -->
    <service
        android:name="io.wazo.callkeep.RNCallKeepBackgroundMessagingService"
        android:exported="false" />

    <!-- ── Service FCM (Firebase Messaging) ───────────────────────────── -->
    <service
        android:name="io.invertase.firebase.messaging.ReactNativeFirebaseMessagingService"
        android:exported="false">
      <intent-filter android:priority="999">
        <action android:name="com.google.firebase.MESSAGING_EVENT" />
      </intent-filter>
    </service>

    <!-- ── Channel de notification pour l'appel entrant ──────────────── -->
    <meta-data
        android:name="com.google.firebase.messaging.default_notification_channel_id"
        android:value="kyc_call_channel"
        tools:replace="android:value" />

    <!-- Icône de notification par défaut -->
    <meta-data
        android:name="com.google.firebase.messaging.default_notification_icon"
        android:resource="@drawable/ic_notification" />

    <!-- ── Service foreground : maintien de l'appel en cours ──────────── -->
    <!-- Type étendu "camera|microphone|phoneCall" : requis à partir d'Android 14
         car le service reste actif pendant que WebRTC capture caméra/micro. -->
    <service
        android:name=".KycForegroundCallService"
        android:foregroundServiceType="camera|microphone|phoneCall"
        android:exported="false"
        android:enabled="true" />

    <!-- ── Receiver : redémarrage après reboot ────────────────────────── -->
    <receiver
        android:name="io.wazo.callkeep.VoiceConnectionServiceReceiver"
        android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
      </intent-filter>
    </receiver>

    <!-- ── ConnectionService Android (CallKeep) ───────────────────────── -->
    <service
        android:name="io.wazo.callkeep.VoiceConnectionService"
        android:label="@string/app_name"
        android:permission="android.permission.BIND_TELECOM_CONNECTION_SERVICE"
        android:exported="true">
      <intent-filter>
        <action android:name="android.telecom.ConnectionService" />
      </intent-filter>
    </service>

  </application>
</manifest>
