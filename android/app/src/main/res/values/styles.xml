<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Theme de base de l'application, requis pour que AAPT2 resolve
         toutes les references "Theme.AppCompat.*" du projet, y compris
         celles venant des dependances tierces (ex: react-native-webrtc). -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@color/primary_dark</item>
    </style>

    <!-- Variante explicite NoActionBar, utile pour des Activity additionnelles
         (ex: ecran d'appel) qui en ont besoin directement. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

</resources>
